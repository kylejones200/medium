data:
  source: null
  generate_synthetic: true
  n_periods: 300
  seed: 42

model:
  lag: 5
  train_size: 0.8
  ensemble_method: "mean"

output:
  figures_dir: "images"
  figure_format: "png"
  figure_dpi: 100

