data:
  source: null
  date_column: "date"
  value_column: "value"
  generate_synthetic: true
  n_periods: 365
  seed: 42

model:
  forecast_horizon: 30
  train_size: 0.8
  use_orbit: true
  use_prophet: true

output:
  figures_dir: "images"
  figure_format: "png"
  figure_dpi: 100

