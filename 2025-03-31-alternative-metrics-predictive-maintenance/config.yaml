data:
  source: null
  generate_synthetic: true
  n_periods: 200
  seed: 42

model:
  sensor_columns:
    - "sensor_1"
    - "sensor_2"
  health_threshold: 0.5
  degradation_window: 10

output:
  figures_dir: "images"
  figure_format: "png"
  figure_dpi: 100

